// GENERATED — do not hand-edit.
// Produce/refresh this file with the better-auth CLI (reads the auth config,
// including the usage plugin's tables: usage / usageEvent / customer):
//
//   bunx @better-auth/cli generate --output packages/db/src/schema/auth.ts
//
// Until generated, the schema is empty (createDb still type-checks).
export {}
